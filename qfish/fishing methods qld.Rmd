---
title: "qfish"
output: html_document
date: "2024-05-18"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

```{r}
library(tidyverse)
```
# load data into R
```{r}
library(dplyr)
```



# renamed a title of a colunm without a space
```{r} 
#qfish <- qfish %>% rename(`fish`=`Fishing Type`)
```

# graphed an initial estimate of the data 
```{r}
#ggplot(data = qfish) + 
 # geom_line (mapping = aes(x=Year, y=Tonnes, colour= fish))

```

# reloaded R with all the data together 
```{r}
#qfishalldata <- read_csv("qfishalldata.csv")
```

# plotted the data into a line graph and added a title 
```{r}
#ggplot(data = qfishalldata) + 
 # geom_line (mapping = aes(x=Year, y=Tonnes, colour= Fishing_Method)) + 
 # ggtitle("QLD Line Fishery")
 
```


### data rangling 
#Loading data into R 
```{r}
fishdata <- read_csv("fishdata.csv",col_names=TRUE)
fishdata
```
```{r}
fishdata1 <- read.csv("fishdata.csv", na = c("N/A",""))
fishdata1
```

# 
```{r}
fishdata2 <- head(fishdata1, -2)
fishdata2
```

```{r}
#fishdata2 <- read.csv("fishdata", na = c("N/A", ""))
#fishdata2
```


```{r}
#fishdata <- fishdata %>% rename(`Beam_Trawl`=`Bream`)
```


```{r}
fishdatalong <- fishdata2 %>%
  pivot_longer(
    cols = -CalendarYear,
    names_to = "Fishing_Type", 
    values_to = "Tonnes"
  )
```

```{r}
#fishdatalong <- fishdatalong %>% mutate(across(everything(), ~ replace_na(., 0)))
#fishdatalong
```

```{r}
fishdatalong$Tonnes<-as.numeric(gsub("[,]","",fishdatalong$Tonnes))
fishdatalong
```


```{r}
ggplot(fishdatalong, aes(x = CalendarYear, y = Tonnes, color = Fishing_Type, group = Fishing_Type)) +
  geom_line() +
  geom_point() +
  labs(title = "Tonnes of catch from different fishing methods QLD", x = "Year", y = "Tonnes") +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```

```{r}
filtered_longdata <- fishdatalong %>%
  filter(Fishing_Type != "Grand Total")
```

```{r}
ggplot(filtered_longdata, aes(x = CalendarYear, y = Tonnes, color = Fishing_Type, group = Fishing_Type)) +
  geom_line() +
  geom_point() +
  labs(title = "Tonnes of catch from different fishing methods QLD", x = "Year", y = "Tonnes") +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```

